---
title: CSS hiện đại
desc: Grid, Flexbox, container query, :has(), color-mix() và các selector mới.
category: code
icon: "{ }"
iconMono: true
tint: "#ffd6e9"
inkOfTint: "#9c2865"
level: mid
minutes: 20
progress: 0
section: khám phá
isNew: true
---

## Grid

### 1. Grid — fr unit là gì?

### 2. Grid — auto-fill vs auto-fit

### 3. Grid — subgrid

## Flexbox

### 4. Flexbox — align-items vs align-content

### 5. Flexbox — flex-grow, flex-shrink, flex-basis

## Responsive

### 6. Container queries — @container

## Selectors

### 7. :has() pseudo-class

### 8. :is() và :where() — khác gì nhau?

## Colors

### 9. color-mix() — pha màu trong CSS

### 10. oklch() — không gian màu hiện đại

## Architecture

### 11. CSS Cascade Layers — @layer

### 12. CSS Custom Properties — scope và inheritance

### 13. clamp() — fluid typography

### 14. logical properties — margin-inline
